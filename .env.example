# Caminho base do armazenamento local do KafkaForge (configurations.json,
# schemas/ em cache e logs/ de operações). Se omitido, usa ~/.kafkaforge.
KAFKAFORGE_HOME=

# Porta em que a aplicação (NiceGUI + FastAPI, mesmo processo) escuta em
# 127.0.0.1.
APP_PORT=8080
